## 项目需求：用 React 开发个人网站，部署到 GitHub Pages

### 技术栈要求
- 框架：React 18+ (使用函数组件 + Hooks)
- 构建工具：Vite (更快) 或 Create React App
- 路由：React Router DOM (实现多页面切换)
- 样式方案：Tailwind CSS / CSS Modules / Styled Components (你选一个)
- 部署：GitHub Pages + GitHub Actions (自动化部署)

### 页面结构（5个核心页面）
导航栏包含以下标签，点击切换内容（无需刷新页面）：

1. **文章页 (/articles)**
   - 展示博客文章列表（标题、摘要、发布时间、标签）
   - 点击文章进入详情页
   - 文章内容用 Markdown 编写，需支持代码高亮
   - 初期预置 3-5 篇示例文章

2. **项目页 (/projects)**
   - 展示我的作品/项目列表
   - 每个项目包含：名称、描述、技术栈、GitHub 链接、预览图（占位图即可）
   - 用卡片网格布局（PC端3列，手机端1列）
   - 初期预置 3-4 个示例项目

3. **技能页 (/skills)**
   - 展示技术栈掌握程度
   - 可用进度条、环形图或标签云形式
   - 分类展示：前端、后端、数据库、工具等
   - 数据用 JS 对象管理，方便后续修改

4. **工具页 (/tools)**
   - 展示我常用的开发工具和软件
   - 每个工具包含：名称、图标（emoji或FontAwesome）、简短说明
   - 分类：编辑器、设计工具、调试工具、效率工具等

5. **关于页 (/about)**
   - 个人简介（头像、姓名、职业）
   - 联系方式（GitHub、邮箱、微博等，用图标链接）
   - 教育/工作经历（时间轴形式）
   - 个人爱好或座右铭

### 设计要求
- 主题：深色模式 + 霓虹蓝/紫色点缀（或者你喜欢的颜色方案）
- 布局：顶部固定导航栏，底部版权信息
- 响应式：手机、平板、电脑完美适配
- 动画：页面切换有淡入淡出效果，卡片悬停有微交互
- 代码整洁：组件拆分合理，注释清晰

### 功能需求
- SPA单页应用，切换标签页不刷新
- 支持浏览器前进/后退按钮
- 文章支持 Markdown 渲染（使用 react-markdown）
- 页面标题随路由变化（React Helmet）
- 访问统计（可选：集成 Google Analytics）

### 数据管理
- 文章数据：存放在 `src/data/articles.js`（数组格式）
- 项目数据：存放在 `src/data/projects.js`
- 技能数据：存放在 `src/data/skills.js`
- 工具数据：存放在 `src/data/tools.js`
- 这样我后续只需修改这些 JS 文件就能更新内容

### 部署要求
- 使用 GitHub Actions 自动部署到 GitHub Pages
- 仓库名：我的用户名.github.io
- 配置 `homepage` 字段处理 GitHub Pages 的二级路径问题
- 提供部署后的访问地址和本地开发命令

### 需要你帮我生成
1. 完整的项目代码（所有组件、页面、配置文件）
2. GitHub Actions 工作流文件 (`.github/workflows/deploy.yml`)
3. 示例数据文件（展示数据结构，我后续替换内容）
4. README.md（包含：项目介绍、技术栈、本地运行命令、部署说明）
5. 详细的操作步骤（从创建项目到部署上线的完整流程）

### 我的技术水平
- 熟悉 React 基础（组件、状态、路由）
- 知道 npm/yarn 基本命令
- 不了解 Vite 配置，需要详细说明

### 其他说明
- 先保证基础功能跑通，样式可以后续调整
- 不需要后端和数据库
- 优先在本地开发环境正常运行，再考虑部署